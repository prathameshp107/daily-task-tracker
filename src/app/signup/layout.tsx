'use client'

/**
 * Layout for the signup page
 * Mirrors the login layout for consistent styling
 */
export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
} 